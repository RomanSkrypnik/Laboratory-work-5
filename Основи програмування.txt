#include "pch.h"
#include <iostream>
int main()
{
	using namespace std;
	float x, y, r, k;
	cout << "Input x\n";
	cin >> x;
	cout << "Input y\n";
	cin >> y;
	cout<<"Input r\n";
	cin>>r;
	k = x * x + y * y;
	if(k <= r) { cout << That point is on the round\n; }
	else{	
	cout << "That point is not on the round\n" << endl;}
	system("pause");
	return 0;
}
